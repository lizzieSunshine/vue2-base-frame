import sessionStorage from '@/libs/sessionStorage';

// sessionStorage缓存的用户信息
const userInfo = sessionStorage.get('userinfo') || {};

const state = {};

const getters = {};

const action = {};

const mutations = {};

export default {
  state,
  getters,
  action,
  mutations
};
